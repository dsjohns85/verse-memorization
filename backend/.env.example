# Database Configuration
# SQLite is used by default (simplest setup)
DATABASE_URL="file:./dev.db"

# For PostgreSQL:
# 1. Copy schema.postgresql.prisma to schema.prisma
# 2. Update DATABASE_URL below and uncomment:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/verse_memorization"

NODE_ENV="development"
PORT=3001

# JWT Configuration (only needed for production)
JWT_SECRET="your-jwt-secret-change-in-production"

# Optional: ESV API Key for automatic verse lookup
# Get your free API key at https://api.esv.org
ESV_API_KEY="your-esv-api-key"
